/**
 * Shared hierarchy contracts for Evolution features.
 *
 * <p>Exclusive phases compete for one world-changing action. Pipeline phases
 * are ordered, non-conflicting stages such as simulation then rendering.
 * Gates protect both kinds, while terminal phases finish or persist state.</p>
 */
package org.evolution.coreparts.hierarchy;
